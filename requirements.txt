# ============================================================================
# このファイルは何をするものか
# ----------------------------------------------------------------------------
# 本プロジェクトの実行に必要なPythonライブラリの一覧です。
# 次のコマンドでまとめてインストールできます。
#
#     pip install -r requirements.txt
#
# 【重要】NVIDIA Jetson で使う場合の注意
#   Jetson では torch / torchvision を pip の標準配布版から入れてはいけません。
#   （標準配布版は x86 PC 用のため Jetson では動きません）
#   NVIDIA が配布している Jetson 専用の wheel を先に入れてから、
#   このファイルの torch / torchvision の2行をコメントアウトして
#   残りをインストールしてください。詳しくは README.md を参照してください。
# ============================================================================

# --- 数値計算の基本ライブラリ（配列の計算に使う） ---
numpy>=1.24

# --- MATLAB形式ファイル(.mat)の読み込みに使う ---
scipy>=1.10

# --- 画像の読み込み・リサイズ・色変換に使う ---
opencv-python>=4.8

# --- ディープラーニングの土台（YOLOv8はこの上で動く） ---
torch>=2.0
torchvision>=0.15

# --- YOLOv8 の本体。学習と推論を行う ---
ultralytics>=8.1

# --- 計測結果のCSVを読み書き・集計するために使う ---
pandas>=2.0

# --- 比較グラフ（PNG画像）を描くために使う ---
matplotlib>=3.7

# --- camera_params.yaml（設定ファイル）を読むために使う ---
PyYAML>=6.0

# --- CPU名・メモリ量などマシン情報を取得するために使う ---
psutil>=5.9
